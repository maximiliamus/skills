interface:
  display_name: "Bump Version"
  short_description: "Update the skills version and changelog safely"
  default_prompt: "Use $bump-version to prepare the next skills repository version and changelog entry."
