interface:
  display_name: "Release"
  short_description: "Validate, tag, and publish a skills release"
  default_prompt: "Use $release to prepare, validate, tag, and atomically push the next skills repository release."
