# Development

## Python Version

Run the test suite only with the latest supported Python version. The current
development and test version is Python 3.14.

```bash
python -m pytest
```

Do not repeat the test suite on older Python versions during routine local
development.
